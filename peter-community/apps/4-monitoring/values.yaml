kube-prometheus-stack:
  grafana:
    adminPassword: "admin"
    service:
      type: ClusterIP

loki-stack:
  loki:
    enabled: true
  promtail:
    enabled: false
  file:
    datasource.yaml: false
