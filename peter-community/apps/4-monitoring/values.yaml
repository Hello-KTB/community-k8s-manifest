kube-prometheus-stack:
  grafana:
    adminPassword: "admin"
    service:
      type: ClusterIP

loki-stack:
  loki:
    enabled: true
    datasource:
      enabled: false
  promtail:
    enabled: false
