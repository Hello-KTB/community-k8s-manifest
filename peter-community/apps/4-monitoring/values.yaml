kube-prometheus-stack:
  grafana:
    adminPassword: "admin"
    service:
      type: ClusterIP

loki-stack:
  loki:
    enabled: true
    datasource:
      isDefault: false
  promtail:
    enabled: false
