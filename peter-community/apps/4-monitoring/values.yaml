kube-prometheus-stack:
  grafana:
    adminPassword: "admin"
    service:
      type: NodePort

loki-stack:
  loki:
    enabled: true
    datasource:
      enabled: false
  promtail:
    enabled: false
  file:
    datasource.yaml: false
  table:
    datasource.yaml: false
